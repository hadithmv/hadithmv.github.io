pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r dist/* public/   # adjust to wherever your built site lives
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
