pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r $(ls -A | grep -v public) public/ 2>/dev/null || true
    - rm -rf public/.git public/.github public/.gitlab-ci.yml
  artifacts:
    paths:
      - public
  only:
    - main
    - master
