name: Mirror to GitLab

on:
  push:
    branches:
      - main

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mirror to GitLab
        env:
          MY_SECRET_TOKEN: ${{ secrets.GITLAB_TOKEN }}
        run: |
          git push --prune --force https://oauth2:${MY_SECRET_TOKEN}@://gitlab.com refs/heads/*:refs/heads/* refs/tags/*:refs/tags/*
